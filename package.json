{
  "name": "lucas-rosat-cv",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Professional CV and portfolio website for Lucas Rosat.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "start": "node scripts/serve.mjs"
  }
}
